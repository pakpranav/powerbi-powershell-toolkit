# Sample data

`training_requests_sample.csv` is fictional data — made up to demonstrate the report's shape (department, category, cost, approval status, quarter) without using any real request or vendor data. Point the actual report at your real SharePoint list; this file exists so someone browsing the repo can see the data shape without needing access to that list.
